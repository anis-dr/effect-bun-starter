# Use OXC for Linting

Effect Bun Starter will use OXC for linting TypeScript and JavaScript code. This keeps linting fast and aligned with the TypeScript monorepo workflow; no OXC dependency or lint configuration is added until the first package needs an actual lint task.
